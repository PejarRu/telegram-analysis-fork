name: Deploy docs

on:
  push:
    branches:
      - master
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'requirements-docs.txt'
      - 'mkdocs-material-deps.txt'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install MkDocs
        run: |
          python -m pip install --upgrade pip
          if [ -f mkdocs-material-deps.txt ]; then pip install -r mkdocs-material-deps.txt; else pip install -r requirements-docs.txt; fi

      - name: Publish to GitHub Pages
        run: mkdocs gh-deploy --force --clean
