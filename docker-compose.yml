services:
  telegram-service:
    build: .
    container_name: telegram-service
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - telegram-data:/app/data

volumes:
  telegram-data:
